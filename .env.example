# Postgres connection string (Supabase, Vercel Postgres, Neon, etc.)
DATABASE_URL="postgresql://user:password@host:5432/aisle?schema=public"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Anthropic — planning chat + document drafting
ANTHROPIC_API_KEY="sk-ant-..."

# Vercel Blob — mood board image uploads
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
